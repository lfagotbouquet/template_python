from setuptools import find_packages, setup

setup(
    name="PROJECT_NAME",
    version="0.0.0",
    description="",
    author="",
    packages=find_packages("src"),
    package_dir={"": "src"},
    install_requires=[],
)
